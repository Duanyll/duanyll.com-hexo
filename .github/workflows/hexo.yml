name: Hexo build

on:
  push:
    branches:
      - master # default branch

jobs:
  pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Cache pandoc deb package
        id: cache-pandoc
        uses: actions/cache@v4
        with:
          path: /tmp/pandoc-3.7.0.2-1-amd64.deb
          key: pandoc-3.7.0.2
      - name: Download pandoc
        if: steps.cache-pandoc.outputs.cache-hit != 'true'
        run: wget https://github.com/jgm/pandoc/releases/download/3.7.0.2/pandoc-3.7.0.2-1-amd64.deb -O /tmp/pandoc-3.7.0.2-1-amd64.deb
      - name: Install pandoc
        run: sudo dpkg -i /tmp/pandoc-3.7.0.2-1-amd64.deb
      - name: Install dependencies
        run: corepack prepare && yarn install --immutable
      - name: Run build
        run: yarn build
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        if: github.ref == 'refs/heads/master'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          cname: duanyll.com
          force_orphan: true